{{- if .Values.kubevela.enabled -}}
apiVersion: plugins.kubegems.io/v1beta1
kind: Plugin
metadata:
  name: kubevela
  namespace: {{ .Values.kubevela.namespace }}
spec:
  enabled: true
  kind: helm
  repo: https://charts.kubevela.net/core
  name: vela-core
  version: {{ .Values.kubevela.version }}
  values:
    multicluster:
      enabled: false
{{- end }}